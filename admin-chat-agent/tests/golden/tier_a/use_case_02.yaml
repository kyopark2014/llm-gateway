# Copyright 2026 © Amazon.com and Affiliates: This deliverable is considered Developed Content as defined in the AWS Service Terms.
use_case_id: "02"
tier: A
name: "어제 평소보다 비싸진 사용자"
question: "어제 평소보다 비싸진 사용자 누구"

expected:
  sql:
    # 7일 평균 vs 어제 비교 — usage_logs 시계열 집계 + users join.
    required_tables: ["usage.usage_logs", "auth.users"]
    required_clauses:
      - "GROUP BY"
      # 시간창 표현 — INTERVAL 리터럴 또는 date 산술/date_trunc 둘 다 허용
      # (correct SQL 이 ::date - N 방식이어도 false-negative 안 나게).
      - "(interval|date_trunc|::date\\s*-|now\\(\\))"
    forbidden_clauses:
      - "INSERT|UPDATE|DELETE|DROP|TRUNCATE|ALTER"
    timezone_hint: "Asia/Seoul"
    # §58: 시간 필터에 AT TIME ZONE 'Asia/Seoul' 강제(UTC 드리프트 회귀 차단).
    timezone_kst: true
  validator:
    verdict: "PASS"
    min_confidence: 0.6
  chart:
    kind: ["table", "bar"]
  agent_path_includes: ["ask_sql_specialist", "ask_validator"]
